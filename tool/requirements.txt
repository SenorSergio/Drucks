# Druckts? Idea-to-Print — Phase 1 backend dependencies
# Pure-pip, no system packages required. Works on Python 3.11–3.13.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Geometry engine (Phase 1): watertight CSG via trimesh + manifold3d
trimesh==4.5.3
manifold3d==3.0.1
numpy==2.2.1
shapely==2.0.6
networkx==3.4.2
mapbox-earcut==1.0.3
lxml==5.3.0

# Text/logo outlines for personalization (keychains, name tags, coasters).
# Used headless for font-path geometry only — no GUI.
matplotlib==3.10.0

# Tests
pytest==8.3.4
